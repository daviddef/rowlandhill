# Handoff — 19 July 2026 (session hit context limit)

Read this first in a new session, then `CLAUDE.md`.

---

## 1. Background jobs that were IN FLIGHT when the session ended

**These may still be running, or may have died with the process. Check before assuming.**

### The big one: v2 harvest
```bash
cd /private/tmp/claude-501/-Users-daviddefranceski-Claude-Projects-Stamp-Scan/755859dc-2150-449f-a116-ff35d7897993/scratchpad
tail -5 v2_all.log          # progress, [n/162]
ls -la v2_all.json          # only written when ALL 162 finish
```
At last check: **25/162 countries, 15,030 records.** Azerbaijan alone returned 3,679.

⚠️ **The script only writes `v2_all.json` at the very end.** If the process died, the work is
lost and it must be re-run — the log tells you how far it got, but the records aren't persisted
incrementally. **Fixing that is the first thing worth doing if it needs re-running**
(`scripts/deep_harvest_v2.py`, `main()`).

Restart if needed:
```bash
nohup python3 ".../scripts/deep_harvest_v2.py" country_issuer_map.json v2_all.json > v2_all.log 2>&1 &
```

Then load it. `scripts/load_deep.py` is the existing loader — **read it before use**, it was
written for the old record shape. Load in 1000-row batches; a single big UNION blows
`max_stack_depth`.

### Two research agents
Both write files directly, so their output survives even if the agent died:
- `docs/research/wns.md` — UPU WNS licence, volume, access
- `docs/research/open-sources.md` — Smithsonian NPM (CC0), US federal works, Nordic/Dutch
  institutions, Europeana

If the files don't exist, the agents died and need relaunching.

---

## 2. The important finding of this session

**The low country coverage was our bug, not a thin source.**

`deep_harvest.py` descended only into Commons subcategories with a year in the name:
```python
return bool(YEAR_RE.search(sub)) or "by year" in sub.lower()
```
Norway files as `1949 stamps of Norway` → matches. Afghanistan files as `Postage stamps of
Afghanistan by date` (**by date**, not by year) plus `Airmail stamps of…` → no match, so descent
stopped at the root and it kept only the 2 loose files. We held exactly 2.

It hid because the symptom looked exactly like a thin source: coverage correlated with country
size, which is plausible enough that nobody checked.

Fixed in `scripts/deep_harvest_v2.py`. Measured:

| | before | after |
|---|---|---|
| Afghanistan | 2 | **1,485** |
| Sri Lanka | 3 | 82 |
| Ghana | 12 | 81 |
| Bangladesh | 3 | 13 |

**Lesson worth keeping:** the plan was to deep-research each country's philatelic history. That
would have produced a well-sourced essay on Afghanistan and missed the answer entirely, which
was a regex. Measure our own pipeline before researching the world.

---

## 3. State of the app

TestFlight **build 3** uploaded (`451c779c-dd5a-40e5-8887-f66a7d4e9447`). Builds 1–3 all
uploaded fine; `./scripts/release_testflight.sh` is one command after bumping
`CURRENT_PROJECT_VERSION` in `project.yml`.

Fixed this session:
- **51-stamp page took ~51 minutes.** Not slow inference — `waitsForConnectivity=true` suppressed
  the fast DNS failure against the undeployed API, so each stamp waited out the 60s resource
  timeout. Now fails fast, stops the page on first unreachable error, ticks progress before each
  stamp rather than after.
- **The results screen lied.** It rendered a hardcoded "Not recognised" for any failure, so 51
  stamps that were never looked up were reported as searched-and-not-found — a false claim about
  our own coverage. Now shows the real reason.

**Still true:** the app cannot identify anything. No trained model
(`StampEmbedder.mlpackage` doesn't exist), no embedding DB
(`stamp_embeddings.sqlite` doesn't exist), no deployed API (`api.rowlandhill.app` does not
resolve — the domain was never bought). Fine for internal testing; do not invite outside testers.

⚠️ **Unverified observation:** some crop thumbnails on the results screen look offset from the
stamps they should contain. Not investigated. Matters once identification works, because bad
crops cost matches.

---

## 4. David's stated priorities (his words, 19 July)

1. **Fix the harvester** — done, running.
2. **Add WNS** — agent dispatched.
3. TBD.

He also said: *"stop giving up so quickly… deep research each country, constantly reiterating on
web searches to grow this again."* Persistence is wanted. But see the lesson in §2 — check our
own pipeline before commissioning research.

**On scraping:** he raised scraping broadly after declining a €100/month API. Position taken,
and worth holding: Commons/PD/institutional sources are fine; **Colnect and StampWorld are not**
— they block it, their terms forbid it, and their catalogue numbering is the IP that's already
queued for the lawyer. Scraping the dataset he just declined to license is the clearest possible
evidence of intent.

---

## 5. Open items for David (not for Claude)

- **IP lawyer**, three questions now: Scott/SG/Michel numbering rights; the "Rowland"
  trademark; whether the PD routes hold (Georgia `PD-GE-exempt`, pre-1976 DPRK, Aruba).
- **Send the drafted Colnect email** (`docs/colnect-licensing-email.md`). A data partnership is
  a different conversation from their €100/mo API price.
- **Buy `rowlandhill.app`** — nothing network-backed in the app works until this exists.
- **Free-tier question, still unanswered:** does one album page of 24 stamps count as 1 scan or
  24? As 1, the free tier gives away the product. As 24, one page exhausts the daily limit twice.

---

## 6. Corpus state

132,351 stamps · 429 succession edges · 201/218 countries — **all pre-harvest numbers.**
Re-measure after loading `v2_all.json`; everything in `docs/coverage.html` and the published
artifact will need updating.

Artifact: https://claude.ai/code/artifact/070e9234-e32d-432e-87b8-f7a455899e08
(update by republishing `scratchpad/coverage_artifact.html` with that URL)

Research written this session: `docs/research/profiles.json` (69 country profiles),
`docs/research/base-catalogue/` (17 dossiers + `_licensing-routes.md`),
`docs/schema/008_missing_succession_edges.sql` (applied).

**Known schema gap, written up in 008:** the graph can't distinguish *issued by* from *used in*.
Georgia reaches 12,723 stamps because its ancestor walk climbs into the USSR. That's why
Faroes→Denmark and Aruba→Curaçao edges were deliberately NOT added. One boolean on
`issuer_succession` plus a parameter on `get_issuer_lineage_by_issuer` would fix it.
